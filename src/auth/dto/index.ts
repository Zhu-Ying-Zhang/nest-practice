// This is quite handy. Because we do not need to import seperately,
// such as `import { AuthDto } from './dto/auth.dto.ts';`
// Instead we can import the file like that: `import { AuthDto } from './dto';`
export * from './auth.dto';
